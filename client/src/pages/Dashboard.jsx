const Dashboard = () => {
    return (
        <div>
            <h1 className="mb-4 text-3xl font-bold">Dashboard</h1>
            <p className="text-muted">Welcome to the main workspace. This is injected into the MainLayout Outlet!</p>
        </div>
    );
};

export default Dashboard;
